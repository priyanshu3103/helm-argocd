server:
  dev:
    enabled: true
    devRootToken: "root"
  ingress:
    enabled: true
    hosts:
      - host: vault.localhost
    annotations:
      traefik.ingress.kubernetes.io/router.tls: "false"
ui:
  enabled: true