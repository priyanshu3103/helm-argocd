applications:
  - name: helm-argocd
    namespace: helm-argocd
    project: default
    repoURL: https://priyanshu3103.github.io/helm-charts   # Helm repo URL
    targetRevision: 0.1.0                                   # Chart version
    chart: helm-argocd                                      # Chart name
    syncPolicy:
      automated:
        prune: true
        selfHeal: true
      syncOptions:
        - CreateNamespace=true